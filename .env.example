# Root-level variables consumed by docker-compose.
# Copy to .env (kept out of git) and fill in.

# Passed as a build arg into the frontend image at build time.
REACT_APP_GOOGLE_CLIENT_ID=

# Backend secrets live in ./backend/.env (see backend/.env.example).
