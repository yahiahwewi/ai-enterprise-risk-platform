# Let's Encrypt certificates are written here by certbot at deploy time.
# This placeholder keeps the directory in git so the docker-compose.prod.yml
# bind-mount (./certbot/conf:/etc/letsencrypt) resolves to a real directory
# instead of Docker auto-creating a root-owned empty one.
